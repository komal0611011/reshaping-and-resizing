# reshaping-and-resizing

# 📘 NumPy Topic 5 – Reshape, Resize & Transpose

This notebook includes my Topic 5 practice for NumPy, focusing on reshaping, resizing, and flattening arrays — a crucial skill for data preparation and transformation.

---

## ✅ Topics Covered

- `reshape()` – Convert 1D to 2D/3D and vice versa
- `ravel()` vs `flatten()` – Flatten arrays (view vs copy)
- `resize()` – In-place reshaping (with repetition if needed)
- `.T` and `transpose()` – Transposing 2D arrays

---

## 📘 Practice Questions Included

- Reshape arrays into various shapes (2×6, 3×4, 4×3)
- Flatten arrays using both `ravel()` and `flatten()`
- Use `resize()` and observe changes (and overflow behavior)
- Transpose arrays and compare shape before & after
- Verify differences between view vs copy behavior

---



